# Default values for minecraft.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# ********REQUIRED********
node:
  hostname: 'k3s01' # Your kube node's hostname
# ************************

minecraft:
  gamemode: 'creative'
  motd: 'Just another Minecraft Server' # Message of the Day
  port: 25565
  node_port: 30565
  whitelist: 'MarioCraft3000,MarioCraft5000,LouCraft2014'

volume:
  storage: 10Gi
  path: '/home/ubuntu/minecraft-data' # Path to wherever you want to save your worldfile

resources:
  limits:
    memory: 8Gi
  requests:
    memory: 6Gi

serviceAccount:
  create: true
